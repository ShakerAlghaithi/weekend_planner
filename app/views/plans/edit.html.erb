<div class="page-header">
  <%= link_to plans_path, :class => 'btn btn-danger' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to @plan, :class => 'btn btn-danger' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>

  <h3>Editing plan</h3>
</div>

<%= render 'form' %>
